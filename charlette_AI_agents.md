
Given that an STL or some 3D file is given to the 3D printer what can we expect when we print it and there are defects?

In this case there have been purposefully introduced defects into the struts, which may appear as clean cuts or might be able to be more or less identified.

If we use some algorithm or model that can predict if there is a given defect in a model, we should also be able to identify design compared to defects.

This might have a smaller impact in a simple straightforward structure such as the lattice octet cubes, but it might be more important or 
scaled to larger projects where the design might appear to be defective when it has a functional purpose.

Furthermore are there sequential changes or noticeable patterns for structs that are defective that can be predicted before it actually happens?

We can have AI agents do analysis which can be based on math or ML for the structure of the print, which for a lattice octet cube might be more straightforward. 

One very important point to remember is the actual application or our work and what would realistically happen. Not to assume they would use our workflow, but to practice
how realistic it might be. It is one thing to have fun and learn new things with the data given but how can we have fun, learn something, and practice using our skills for work.



Another thing with the files we are given, is that there are also possible deviations in degrees of how the CT scans are oriented and generated.

For the post analysis pipelines, it is to help analyze what possible have went wrong in the first place and also to validate the earlier findings 


Also, we can possibly do token analysis of various AI Agent pipelines. To observe the usage, and possible costs associated with the pipeline to analyze the 3D models. 


Other things to think about is further validation and performance of the model which might include several different techniques but i just really like cross-validation specifically if that is possible. 



For my teammate let's introduce this design as pre and post analysis of the structures found itself.

## Let's also define 3 main research objectives:

1. Can the structs or design of a given 3D model, assumably as an .stl file be analyzed to give us meaningful insights prior to printing the model for defectiveness?
2. After printing the 3D model, can we create accurate representations and explanations for the performance and quality? Also can this be used to validate the pre-model analysis?
3. How can we make our AI Agentic workflow robust, efficient, and meaningful for analyzing 3D printed models?

Bonus Question: Can we do real-time analysis and LLM workflow when the thing is printing? Can we simulate that?

## Potential tasks:

Okay we have to somehow trasnform the structs itself to usable datapoints. It might take a long time analyze straigtht up 3D models, so how can we transform that data into usable datapoints. Both for our use case here, and for future shapes and pipelines. 

A proposal we can do is create a pre and post analysis pipeline of the .STL file itself which would analyze the potential or predicted weak points of the print. This also 
includes the analysis of the struct widths and whatnot. (Which can be shown as a report in the form of a matrix of the histogram). 

It might be beneficial to create other "test" objects or simulated objects (possibly through codex doing it) and see how that would interact compared to the cube itself





